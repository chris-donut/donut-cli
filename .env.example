# ============================================================================
# Donut CLI Configuration
# ============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Or run the setup wizard:
#   bun run build && node dist/index.js setup
# ============================================================================

# ----------------------------------------------------------------------------
# REQUIRED: Anthropic API Key
# ----------------------------------------------------------------------------
# Get your API key at: https://console.anthropic.com/settings/keys
# This is required for AI-powered features (chat, strategy building, analysis)
# Demo mode works without this key.
ANTHROPIC_API_KEY=

# ----------------------------------------------------------------------------
# OPTIONAL: AI Model Settings
# ----------------------------------------------------------------------------
# Claude model to use (default: claude-sonnet-4-20250514)
# Options: claude-sonnet-4-20250514, claude-3-5-sonnet-20241022, claude-3-opus-20240229
# CLAUDE_MODEL=claude-sonnet-4-20250514

# Maximum turns per agent conversation (default: 30)
# MAX_TURNS=30

# Maximum budget per session in USD (default: 10.0)
# MAX_BUDGET_USD=10.0

# ----------------------------------------------------------------------------
# OPTIONAL: Backend Servers
# ----------------------------------------------------------------------------

# Donut Agents Backend - AI trading agents with LLM decisions (port 8080)
# Start with: go run main.go (in donut-agents-backend directory)
# DONUT_AGENTS_URL=http://localhost:8080
# DONUT_AGENTS_AUTH_TOKEN=your-jwt-token

# Donut Backend - Solana DeFi portfolio & transactions (port 3000)
# Start with: pnpm dev (in donut-backend directory)
# DONUT_BACKEND_URL=http://localhost:3000
# DONUT_BACKEND_AUTH_TOKEN=your-jwt-token

# Hummingbot API - Multi-exchange trading & bot orchestration (port 8000)
# Start with: docker compose up -d (in hummingbot-api directory)
# HUMMINGBOT_URL=http://localhost:8000
# HUMMINGBOT_USERNAME=admin
# HUMMINGBOT_PASSWORD=admin

# ----------------------------------------------------------------------------
# OPTIONAL: Python Harness (for advanced backtesting)
# ----------------------------------------------------------------------------
# Path to trading-agent-harness directory
# HARNESS_WORKING_DIR=/path/to/trading-agent-harness

# Python executable path (default: python3)
# PYTHON_PATH=python3

# ----------------------------------------------------------------------------
# OPTIONAL: Donut Browser UI
# ----------------------------------------------------------------------------
# URL for the Donut Browser companion app
# DONUT_BROWSER_URL=http://localhost:3001

# ----------------------------------------------------------------------------
# OPTIONAL: Notifications (Telegram)
# ----------------------------------------------------------------------------
# Telegram bot token for trade alerts and notifications
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=

# ----------------------------------------------------------------------------
# OPTIONAL: Social Sentiment (for market analysis)
# ----------------------------------------------------------------------------
# Twitter/X API bearer token for sentiment analysis
# TWITTER_BEARER_TOKEN=

# Discord bot token for community sentiment
# DISCORD_BOT_TOKEN=

# Neynar API key for Farcaster social signals (donut_trending, donut_search_mentions)
# Get your API key at: https://neynar.com
# NEYNAR_API_KEY=

# ----------------------------------------------------------------------------
# OPTIONAL: Risk Management Limits
# ----------------------------------------------------------------------------
# Maximum position size in USD (default: 10000)
# MAX_POSITION_SIZE_USD=10000

# Maximum daily loss in USD (default: 1000)
# MAX_DAILY_LOSS_USD=1000

# Maximum concurrent positions (default: 5)
# MAX_OPEN_POSITIONS=5

# Require manual confirmation for trades (default: true)
# REQUIRE_TRADE_CONFIRMATION=true

# Comma-separated list of blacklisted symbols
# BLACKLISTED_SYMBOLS=

# ----------------------------------------------------------------------------
# OPTIONAL: On-Chain Trading (Solana)
# ----------------------------------------------------------------------------
# SECURITY: Private keys are loaded only at execution time and NEVER logged.
# Ensure this file has restrictive permissions: chmod 600 .env

# Solana wallet private key (base58-encoded)
# Export from Phantom: Settings > Security & Privacy > Export Private Key
# SOLANA_PRIVATE_KEY=

# Solana RPC endpoint (default: https://api.mainnet-beta.solana.com)
# For better performance, use a paid RPC provider like Helius or QuickNode
# SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# ----------------------------------------------------------------------------
# OPTIONAL: On-Chain Trading (Base)
# ----------------------------------------------------------------------------
# SECURITY: Private keys are loaded only at execution time and NEVER logged.
# Ensure this file has restrictive permissions: chmod 600 .env

# Base wallet private key (hex-encoded, with or without 0x prefix)
# Export from MetaMask: Account details > Show private key
# BASE_PRIVATE_KEY=

# Base RPC endpoint (default: https://mainnet.base.org)
# For better performance, use Alchemy, Infura, or QuickNode
# BASE_RPC_URL=https://mainnet.base.org

# Use Base Sepolia testnet instead of mainnet (default: false)
# BASE_TESTNET=false

# 0x API key for Base chain swaps (optional, but recommended for production)
# Get your API key at: https://0x.org/docs/introduction/getting-started
# ZERO_X_API_KEY=

# ----------------------------------------------------------------------------
# OPTIONAL: Perpetual Futures Trading (Hyperliquid)
# ----------------------------------------------------------------------------
# SECURITY: Private keys are loaded only at execution time and NEVER logged.
# Ensure this file has restrictive permissions: chmod 600 .env

# Hyperliquid wallet private key (hex-encoded, with or without 0x prefix)
# This is the same key format as your EVM wallet (MetaMask, etc.)
# Hyperliquid uses the Arbitrum network for deposits/withdrawals
# HYPERLIQUID_PRIVATE_KEY=

# Use Hyperliquid testnet instead of mainnet (default: false)
# Testnet: https://app.hyperliquid-testnet.xyz
# Mainnet: https://app.hyperliquid.xyz
# HYPERLIQUID_TESTNET=false

# ----------------------------------------------------------------------------
# OPTIONAL: Prediction Markets Trading (Polymarket)
# ----------------------------------------------------------------------------
# SECURITY: Private keys are loaded only at execution time and NEVER logged.
# Ensure this file has restrictive permissions: chmod 600 .env

# Polygon wallet private key (hex-encoded, with or without 0x prefix)
# Polymarket uses USDC on Polygon network for settlement
# Export from MetaMask: Account details > Show private key
# POLYGON_PRIVATE_KEY=

# Optional: Polymarket API credentials (will be derived from wallet if not set)
# These are generated when you first connect to Polymarket
# POLYMARKET_API_KEY=
# POLYMARKET_SECRET=
# POLYMARKET_PASSPHRASE=

# ----------------------------------------------------------------------------
# OPTIONAL: Terminal Settings
# ----------------------------------------------------------------------------
# Logging level: debug, info, warn, error (default: info)
LOG_LEVEL=info

# Directory for session data (default: .sessions)
SESSION_DIR=.sessions
